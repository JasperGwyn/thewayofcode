{
  "name": "electron-break-timer",
  "version": "1.0.0",
  "description": "Electron app that runs in system tray and triggers breaks every 30 minutes",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "dev": "tsc && npm run copy-overlay-assets && cross-env NODE_ENV=development electron dist/main.js",
    "copy-overlay-assets": "powershell -Command \"New-Item -ItemType Directory -Force dist/overlay/ui; Copy-Item src/overlay/ui/index.html dist/overlay/ui/; Copy-Item src/overlay/ui/styles.css dist/overlay/ui/; New-Item -ItemType Directory -Force dist/settings/ui; Copy-Item src/settings/ui/index.html dist/settings/ui/; Copy-Item src/settings/ui/styles.css dist/settings/ui/\"",
    "build": "tsc && npm run copy-overlay-assets",
    "package": "npm run build && electron-packager . \"Break Timer\" --platform=win32 --arch=x64 --out=dist-installer --overwrite",
    "zip": "powershell \"Compress-Archive -Path 'dist-installer\\Break Timer-win32-x64' -DestinationPath 'dist-installer\\Break Timer v1.0.0.zip' -Force\"",
    "dist": "npm run package && npm run zip",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix"
  },
  "build": {
    "appId": "com.thewayofcode.break-timer",
    "productName": "Break Timer",
    "directories": {
      "output": "dist-installer"
    },
    "files": [
      "dist/**/*",
      "assets/**/*"
    ],
    "win": {
      "target": "nsis",
      "sign": false,
      "verifyUpdateCodeSignature": false
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "cross-env": "^10.1.0",
    "electron": "^30.0.0",
    "electron-builder": "^23.6.0",
    "electron-packager": "^17.1.2",
    "electron-reload": "^2.0.0-alpha.1",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
